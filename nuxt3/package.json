{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev & json-server ./json-server/db.json --port 3001",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.1.3",
    "nuxt": "3.0.0"
  },
  "dependencies": {
    "@formkit/nuxt": "^1.0.0-beta.12",
    "concurrently": "^7.6.0",
    "daisyui": "^2.42.1",
    "json-server": "^0.17.1"
  }
}
